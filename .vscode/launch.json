{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "node --inspect server.js",
            "name": "Launch Program",
            "program": "${workspaceFolder}\\server.js"
        }
    ]
}